#include<stdio.h>

int main(void)
{
    int x;
        printf("dwse enan akeraio : ");
        scanf("%d", &x);

        if (x>0)
        {
           printf("o arithmos einai thetikos");
        }

        else
            printf("o arithos einai mh thetikos");

    return 0;
}
